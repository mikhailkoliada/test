on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Run a multi-line script
        run: |
          gcc -v -xc++ /dev/null -fsyntax-only
