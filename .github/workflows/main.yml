name: Screencapture

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  latest:
    runs-on: macos-latest

    steps:
    - run: screencapture -T10 -V 10 foo.mov
    - uses: actions/upload-artifact@v3
      with:
        name: latest
        path: foo.mov
